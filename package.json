{
  "name": "flashpod",
  "version": "0.1.0",
  "description": "TypeScript-first wrapper for RunPod Flash: typed config, generated Python bridge, CLI lifecycle, and endpoint client.",
  "type": "module",
  "license": "MIT",
  "author": "Jalil <reachjalil@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reachjalil/flashpod.git"
  },
  "bugs": {
    "url": "https://github.com/reachjalil/flashpod/issues"
  },
  "homepage": "https://github.com/reachjalil/flashpod#readme",
  "keywords": [
    "runpod",
    "flash",
    "typescript",
    "serverless",
    "gpu",
    "ai",
    "python-bridge"
  ],
  "bin": {
    "flashpod": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "docs",
    "examples",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "npm run build && node --test test/*.test.mjs",
    "test:e2e": "npm run build && node --test test/*.e2e.mjs",
    "test:package": "npm run build && node scripts/package-smoke.mjs",
    "check": "npm run typecheck && npm test && npm run test:package",
    "prepare": "npm run build",
    "prepublishOnly": "npm run check"
  },
  "dependencies": {
    "jiti": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.8",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.11"
  },
  "publishConfig": {
    "access": "public"
  }
}
